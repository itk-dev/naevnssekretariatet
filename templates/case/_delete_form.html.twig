<form method="post" action="{{ path('case_delete', {'id': case_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ case_entity.id) }}">
    <button class="btn">Delete</button>
</form>
