{% trans_default_domain 'case' %}

<div class="modal fade" id="move_case">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="assignCaseworkerModalLabel">{% trans %}Move case to different board{% endtrans %}</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                {{ form_start(move_form, {'action': path('case_move', {'id': case.id})}) }}
                {{ form_widget(move_form) }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">{% trans %}Close{% endtrans %}</button>
                <button type="submit" class="btn btn-primary">{% trans %}Confirm{% endtrans %}</button>
                {{ form_end(move_form) }}
            </div>
        </div>
    </div>
</div>
